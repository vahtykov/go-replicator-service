---
alwaysApply: false
---
1) Сделать SQL скрипт триггера, который отслеживает все DML операции на таблице, и записывает их в таблицу replication_queue. Также, скрипт должен предотвращать проблему петли репликации.

2) 